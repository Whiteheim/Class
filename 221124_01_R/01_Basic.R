print("hello")
# R 
#   => 통계를 위한 프로그래밍 언어
#   => 수치를 계산하고, 그래프 등의 시각화 기능을 가진 언어
# 초창기에는 'R'의 비중이 매우 커서 대부분이 R을 사용했음
# Python에서 기능들을 점점 추가하면서 서서히 Python으로 넘어가기 시작
# 하지만 R의 점유율이 완전히 사라지는것은 아니기 때문에 알아두면 좋음

# R을 쓰는 이유?
# => 기본적인 통계, 시각화가 가능 -> R에 통계용어가 많이 존재
# => 통계, 시각화 패키지들이 다양하게 존재
# => 무료, 오픈소스
# => Data Frame이라는 자료형이 존재

# Data Frame
# => Table형식의 데이터 표현, 다루는 방식
# => 여러 행, 열로 구성이 된 2차원 형식의 데이터 구조를 가짐 
# => RDBMS(ex: Oracle)의 테이블 느낌이 강함
# => 정수, 실수, 문자열, ... 의 여러 데이터를 처리가능

# 단축키
#   한 줄 실행 : Ctrl + Enter
#   전체 실행 : Ctrl + Shift + Enter
#   한 줄 지우기 : Ctrl + D
#   Console Clear (콘솔 비우기) : Ctrl + L
#   주석처리 : Ctrl + Shift + C
#               => Tools - Modify Keyboard Shortcuts - comment - Ctrl + / 로 변경
#   기능 목록 보기 : Ctrl + Space
#   자동완성 : Tab



























